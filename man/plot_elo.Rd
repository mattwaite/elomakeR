% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_elo}
\alias{plot_elo}
\title{Plot Elo rating trajectories}
\usage{
plot_elo(elo, teams = NULL)
}
\arguments{
\item{elo}{A tibble returned by \code{\link[=calculate_elo]{calculate_elo()}}.}

\item{teams}{Optional character vector of team names to restrict the
plot to. Default (\code{NULL}) plots every team, which is often too
crowded to be useful.}
}
\value{
A ggplot object.
}
\description{
Draws each team's rating over time as a step line. Requires the
ggplot2 package. Registered as a \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} method for
\code{elo_tbl} objects as well, so \code{autoplot(elo)} also works once
ggplot2 is loaded.
}
\examples{
\donttest{
data(basketball_games)
elo <- calculate_elo(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_elo(elo, teams = c("Duke", "Michigan"))
}
}

}
