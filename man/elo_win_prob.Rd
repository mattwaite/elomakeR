% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/win_probability.R
\name{elo_win_prob}
\alias{elo_win_prob}
\title{Elo win probability}
\usage{
elo_win_prob(rating_a, rating_b, home_advantage = 0, scale = 400)
}
\arguments{
\item{rating_a, rating_b}{Numeric vectors of Elo ratings (recycled
against each other).}

\item{home_advantage}{Rating points to add to \code{rating_a} before
computing the probability. Default \code{0}. Pass a negative value, or
add it to \code{rating_b} instead, to give the advantage to team B.}

\item{scale}{The Elo scale factor. Default \code{400}, matching
\code{\link[=calculate_elo]{calculate_elo()}}'s default.}
}
\value{
A numeric vector of probabilities that team A wins (ties, if
any, are not represented -- this is strictly P(A beats B) under the
Elo model).
}
\description{
Computes the probability that team A beats team B given their Elo
ratings, using the standard logistic Elo formula. Useful for
generating predictions from a fitted rating table, and used
internally by \code{\link[=calculate_elo]{calculate_elo()}}.
}
\examples{
elo_win_prob(1600, 1500)
elo_win_prob(1600, 1500, home_advantage = 100)

}
