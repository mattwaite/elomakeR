% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{suggest_learning_rate}
\alias{suggest_learning_rate}
\title{Suggest a learning rate (K-factor) by grid search}
\usage{
suggest_learning_rate(
  data,
  ...,
  k_grid = seq(4, 60, by = 2),
  metric = c("log_loss", "accuracy")
)
}
\arguments{
\item{data}{A data frame of games, as for \code{\link[=calculate_elo]{calculate_elo()}}.}

\item{...}{Column mappings and other arguments forwarded to
\code{\link[=calculate_elo]{calculate_elo()}} (e.g. \verb{date =}, \verb{team1 =}, \verb{team2 =}, \verb{score1 =},
\verb{score2 =}, \verb{home_team =}, \verb{season =}, \verb{mov_adjustment =}). Do not
pass \code{k} here -- it is supplied by \code{k_grid}.}

\item{k_grid}{Numeric vector of candidate \code{k} values to try. Default
\code{seq(4, 60, by = 2)}.}

\item{metric}{One of \code{"log_loss"} (default; lower is better) or
\code{"accuracy"} (higher is better; computed only over decisive, i.e.
non-tied, games).}
}
\value{
A list with \code{best_k}, \code{best_score}, \code{metric}, and \code{grid} (a
tibble of every \code{k} tried and its score).
}
\description{
Tries each value in \code{k_grid}, runs \code{\link[=calculate_elo]{calculate_elo()}} over \code{data} with
that \code{k} (all other arguments held fixed via \code{...}), scores the
resulting pre-game win probabilities against the actual results, and
reports the best-scoring value.
}
\details{
Pass a full prior season (or several seasons) to pick a \code{k} to carry
into a new season, or pass the current season's games so far to tune
\code{k} on the fly as the season progresses.
}
\examples{
\donttest{
data(basketball_games)
suggest_learning_rate(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site,
  k_grid = seq(10, 40, by = 10)
)
}

}
