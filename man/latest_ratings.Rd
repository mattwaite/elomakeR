% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{latest_ratings}
\alias{latest_ratings}
\title{Extract the most recent rating for each team}
\usage{
latest_ratings(elo, season = NULL)
}
\arguments{
\item{elo}{A tibble returned by \code{\link[=calculate_elo]{calculate_elo()}}.}

\item{season}{Optional. If supplied, restricts to games in this
season (matching the \code{season} column) before finding each team's
latest rating within that season.}
}
\value{
A tibble with one row per team: \code{team}, \code{rating}, \code{date} (the
date of that team's most recent game), sorted by \code{rating}
descending.
}
\description{
Given the long-format output of \code{\link[=calculate_elo]{calculate_elo()}}, returns each
team's latest rating -- i.e. a current standings table.
}
\examples{
\donttest{
data(basketball_games)
elo <- calculate_elo(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site
)
latest_ratings(elo)
}

}
