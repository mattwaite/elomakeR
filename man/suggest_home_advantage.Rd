% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{suggest_home_advantage}
\alias{suggest_home_advantage}
\title{Suggest a home-advantage value by grid search}
\usage{
suggest_home_advantage(
  data,
  ...,
  home_advantage_grid = seq(0, 200, by = 10),
  metric = c("log_loss", "accuracy")
)
}
\arguments{
\item{data}{A data frame of games, as for \code{\link[=calculate_elo]{calculate_elo()}}.}

\item{...}{Column mappings and other arguments forwarded to
\code{\link[=calculate_elo]{calculate_elo()}} (e.g. \verb{date =}, \verb{team1 =}, \verb{team2 =}, \verb{score1 =},
\verb{score2 =}, \verb{home_team =}, \verb{season =}, \verb{mov_adjustment =}). Do not
pass \code{k} here -- it is supplied by \code{k_grid}.}

\item{home_advantage_grid}{Numeric vector of candidate home-advantage
values to try. Default \code{seq(0, 200, by = 10)}.}

\item{metric}{One of \code{"log_loss"} (default; lower is better) or
\code{"accuracy"} (higher is better; computed only over decisive, i.e.
non-tied, games).}
}
\value{
A list with \code{best_home_advantage}, \code{best_score}, \code{metric}, and
\code{grid} (a tibble of every value tried and its score).
}
\description{
Tries each value in \code{home_advantage_grid}, runs \code{\link[=calculate_elo]{calculate_elo()}} over
\code{data} with that home advantage (all other arguments held fixed via
\code{...}), scores the resulting pre-game win probabilities against the
actual results, and reports the best-scoring value.
}
\details{
As with \code{\link[=suggest_learning_rate]{suggest_learning_rate()}}, pass historical (e.g. prior-season)
data to get a value to use going forward, or the current season's
games so far to tune on the fly.
}
\examples{
\donttest{
data(basketball_games)
suggest_home_advantage(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site,
  home_advantage_grid = seq(0, 150, by = 50)
)
}

}
