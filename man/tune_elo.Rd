% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{tune_elo}
\alias{tune_elo}
\title{Jointly tune learning rate and home advantage by grid search}
\usage{
tune_elo(
  data,
  ...,
  k_grid = seq(4, 60, by = 4),
  home_advantage_grid = seq(0, 200, by = 25),
  metric = c("log_loss", "accuracy")
)
}
\arguments{
\item{data}{A data frame of games, as for \code{\link[=calculate_elo]{calculate_elo()}}.}

\item{...}{Column mappings and other arguments forwarded to
\code{\link[=calculate_elo]{calculate_elo()}} (e.g. \verb{date =}, \verb{team1 =}, \verb{team2 =}, \verb{score1 =},
\verb{score2 =}, \verb{home_team =}, \verb{season =}, \verb{mov_adjustment =}). Do not
pass \code{k} here -- it is supplied by \code{k_grid}.}

\item{k_grid}{Numeric vector of candidate \code{k} values to try. Default
\code{seq(4, 60, by = 2)}.}

\item{home_advantage_grid}{Numeric vector of candidate home-advantage
values to try. Default \code{seq(0, 200, by = 25)}.}

\item{metric}{One of \code{"log_loss"} (default; lower is better) or
\code{"accuracy"} (higher is better; computed only over decisive, i.e.
non-tied, games).}
}
\value{
A list with \code{best_k}, \code{best_home_advantage}, \code{best_score},
\code{metric}, and \code{grid} (a tibble of every combination tried and its
score).
}
\description{
Like \code{\link[=suggest_learning_rate]{suggest_learning_rate()}} and \code{\link[=suggest_home_advantage]{suggest_home_advantage()}} but
searches both parameters at once, which matters when they interact
(e.g. a higher home advantage changes which \code{k} best fits the data).
}
\examples{
\donttest{
data(basketball_games)
tune_elo(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site,
  k_grid = seq(10, 30, by = 10),
  home_advantage_grid = seq(0, 100, by = 50)
)
}

}
