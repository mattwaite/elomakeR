% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_elo.R
\name{calculate_elo}
\alias{calculate_elo}
\title{Calculate Elo ratings over a sequence of games}
\usage{
calculate_elo(
  data,
  date,
  team1,
  team2,
  score1,
  score2,
  home_team = NULL,
  neutral = NULL,
  season = NULL,
  game_id = NULL,
  k = 20,
  home_advantage = 100,
  initial_rating = 1500,
  scale = 400,
  mov_adjustment = c("none", "log"),
  mov_fun = NULL,
  carryover = c("continuous", "reset", "regress"),
  regress_pct = 1/3
)
}
\arguments{
\item{data}{A data frame of games, one row per game.}

\item{date}{<\code{\link[rlang:args_data_masking]{data-masking}}> Column giving
the date (or any orderable value) of each game.}

\item{team1, team2}{<\code{\link[rlang:args_data_masking]{data-masking}}> Columns
naming the two teams in each game.}

\item{score1, score2}{<\code{\link[rlang:args_data_masking]{data-masking}}>
Numeric columns giving each team's score. The winner is whichever
score is larger; equal scores are treated as a tie.}

\item{home_team}{<\code{\link[rlang:args_data_masking]{data-masking}}>
Optional column giving the name of the home team for each game (its
value should equal either \code{team1} or \code{team2} on that row). Leave as
\code{NULL} (the default) to treat every game as a neutral site, i.e. to
apply no home advantage anywhere.}

\item{neutral}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional
logical column. Rows where this is \code{TRUE} get no home advantage even
if \code{home_team} is set for that row.}

\item{season}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional
column identifying the season each game belongs to. Required if
\code{carryover} is \code{"reset"} or \code{"regress"}. Assumes seasons do not
temporally overlap once \code{data} is sorted by \code{date}.}

\item{game_id}{<\code{\link[rlang:args_data_masking]{data-masking}}> Optional
column with an identifier for each game, carried through to the
output. Defaults to the row number in \code{data}.}

\item{k}{Learning rate (K-factor): how many rating points are at stake
in a single game. Higher values make ratings react faster to recent
results. Default \code{20}. See \code{\link[=suggest_learning_rate]{suggest_learning_rate()}}.}

\item{home_advantage}{Rating-point bonus added to the home team's
rating when computing win probability (it is not added to its
stored rating). Default \code{100}. Set to \code{0} to disable. See
\code{\link[=suggest_home_advantage]{suggest_home_advantage()}}.}

\item{initial_rating}{Rating assigned to a team the first time it
appears. Default \code{1500}.}

\item{scale}{The Elo scale factor controlling how much a rating gap
translates into win probability. Default \code{400}, the standard Elo
value (a 400-point gap implies a 10:1 favorite).}

\item{mov_adjustment}{One of \code{"none"} (default) or \code{"log"}. \code{"log"}
scales \code{k} up for lopsided wins and down for narrow ones, using the
margin-of-victory formula popularized by FiveThirtyEight:
\verb{log(|score1 - score2| + 1) * 2.2 / (0.001 * |elo_diff| + 2.2)}.
Always treated as \code{1} (no adjustment) for tied games. Ignored
entirely if \code{mov_fun} is supplied.}

\item{mov_fun}{Optional custom margin-of-victory function,
\verb{function(score1, score2, rating1, rating2)}, returning a numeric
multiplier applied to \code{k}. Overrides \code{mov_adjustment} when supplied.}

\item{carryover}{One of \code{"continuous"} (default: ratings simply carry
forward across seasons unchanged), \code{"reset"} (every team's rating is
set back to \code{initial_rating} at the start of each new season), or
\code{"regress"} (each team's rating moves a fraction \code{regress_pct} of
the way back to \code{initial_rating} at the start of each new season).
Requires \code{season} to be mapped when not \code{"continuous"}.}

\item{regress_pct}{Fraction between 0 and 1 to regress ratings toward
\code{initial_rating} at each season boundary when \code{carryover = "regress"}. Default \code{1/3}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}, subclassed \code{"elo_tbl"}, with one row per
team per game, sorted by date: \code{game_id}, \code{date}, \code{season}, \code{team},
\code{opponent}, \code{is_home}, \code{score_for}, \code{score_against}, \code{result} (\code{1}
win, \code{0.5} tie, \code{0} loss), \code{win_prob} (pre-game probability of
winning), \code{pre_elo}, \code{post_elo}, \code{elo_change}.
}
\description{
Runs the Elo rating system over a data frame of games, processing them
in chronological order, and returns a long-format tibble with one row
per team per game showing that team's rating immediately before and
after the game.
}
\details{
Columns in \code{data} are mapped with tidy evaluation (data masking), so
this works with any input schema -- the columns do not need to be
named \code{team1}/\code{team2}/etc. See the \code{home_team} and \code{season} arguments
below for how to adapt the function to sports without a home team or
without a season structure.
}
\examples{
\donttest{
data(basketball_games)
elo <- calculate_elo(
  basketball_games,
  date = date, team1 = home_location, team2 = away_location,
  score1 = home_score, score2 = away_score,
  home_team = home_location, neutral = neutral_site
)
latest_ratings(elo)

# a messier, non-standard schema works the same way: team2 is the home
# team, and the outcome is given as sets won rather than a single score
data(volleyball_sets)
calculate_elo(
  volleyball_sets,
  date = date, team1 = team1, team2 = team2,
  score1 = team1_sets_won, score2 = team2_sets_won,
  home_team = team2
)
}

}
